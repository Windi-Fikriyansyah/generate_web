version: '3.8'

services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-admin123}
      POSTGRES_DB: ${POSTGRES_DB:-db_generate}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    deploy:
      resources:
        limits:
          memory: 4G
    networks:
      - webnet

  redis:
    image: redis:7-alpine
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G
    networks:
      - webnet

  backend:
    build: ./backend
    restart: always
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-admin123}@db:5432/${POSTGRES_DB:-db_generate}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - FONT_PATH=fonts/Roboto-Bold.ttf
      - SECRET_KEY=${SECRET_KEY:-b9e31d3f9b8c4d29a5f4e6d7c8b9a1e2f3g4h5i6j7k8l9m0n1o2p3q4r5s6t7u8}
    volumes:
      - ./backend/storage:/app/storage
    depends_on:
      - db
      - redis
    deploy:
      resources:
        limits:
          memory: 4G
    networks:
      - webnet

  worker:
    build: ./backend
    restart: always
    # Concurrency set to 8 to match 8 vCPU cores
    command: celery -A celery_app worker --loglevel=info --concurrency=8
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-admin123}@db:5432/${POSTGRES_DB:-db_generate}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - FONT_PATH=fonts/Roboto-Bold.ttf
    volumes:
      - ./backend/storage:/app/storage
    depends_on:
      - db
      - redis
    deploy:
      resources:
        limits:
          # High memory allocation for large image processing
          memory: 20G
    networks:
      - webnet

  frontend:
    build: ./frontend
    restart: always
    depends_on:
      - backend
    deploy:
      resources:
        limits:
          memory: 1G
    networks:
      - webnet

  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./backend/storage:/app/storage:ro
    depends_on:
      - frontend
      - backend
    deploy:
      resources:
        limits:
          memory: 512M
    networks:
      - webnet

volumes:
  postgres_data:
  certbot_www:
  certbot_conf:

networks:
  webnet:
